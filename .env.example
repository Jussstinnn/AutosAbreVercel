# Modo de datos: "mock" (default) o "api"
VITE_DATA_SOURCE=mock

# Base URL del backend cuando VITE_DATA_SOURCE=api
# VITE_API_BASE_URL=https://api.tudominio.com

# Opcional: "same-origin" | "include" | "omit"
# VITE_API_CREDENTIALS=same-origin
